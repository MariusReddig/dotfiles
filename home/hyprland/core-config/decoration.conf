misc {
disable_hyprland_logo = true
}

# Workspace decoration
general {
  gaps_in = 5								# gap-size between windows
  gaps_out = 11							# gap-size between screen-edges
  border_size = 2
  col.active_border = rgba(A1BDCEFF)		# active window-border
  col.inactive_border = rgba(27272EFF)				# inactive window-border
  layout = dwindle							# window layout
  snap {
	  enabled = true
	  window_gap = 10
	  monitor_gap = 10
	  border_overlap = false
	}
}

# Workspace tiling
dwindle {
      force_split                  = 0
      preserve_split               = false
      smart_split                  = false
      smart_resizing               = true
      permanent_direction_override = false
      special_scale_factor         = 1
      split_width_multiplier       = 1.0
      use_active_for_splits        = true
      default_split_ratio          = 1.0
      split_bias                   = 0
      precise_mouse_move           = false
}

# Window decoration
decoration {
	rounding = 5				# border rounding

	shadow {
    enabled = false					# drop shadow
    range = 4								# drop shadow size in px
    render_power = 3				# shadow power falloff
    color = rgba(1a1a1aee)	# shadow collor
	}

	blur {
    enabled = true	# blur
    size = 3				# blur distance
		passes = 1			# passes to perform
	}

}

# Window animations
animations {
    enabled = yes

    bezier = myBezier, 0.22, 1.0, 0.36, 1

    animation = windows, 1, 7, myBezier
    animation = windowsOut, 1, 7, default, slide
    animation = border, 1, 20, default
    animation = borderangle, 1, 8, default
    animation = fade, 1, 7, default
    animation = workspaces, 1, 6, default
}

###---Game-Workspace-Config---###
# workspace = name:game, persistent:true
# hl.window_rule({ name = "game-workspace(steamapps)", match = {initialClass:^(steam_app_.*)$}, workspace name:game,})
# windowrule = workspace name:game, initialClass:^(gamescope)$
# windowrule = windowrule = tile, initialClass:^(steam_app_.*)$
# windowrule = workspace name:game, initialClass:^(Tabletop Simulator.*)$
# windowrule = tile, initialClass:(Tabletop Simulator.*)
# windowrule = workspace name:game, initialClass:^(forge-view-Main)$
# # windowrule = fullscreenstate 0, initialClass:^(forge-view-Main)$
# # windowrule = tile, title:^(Forge: 2.0.08-SNAPSHOT-01.05)$
# # windowrule = fullscreenstate 0, title:^(Forge: 2.0.08-SNAPSHOT-01.05)$
# windowrule = tile, initialClass:(Ryujinx)
# windowrule = workspace name:game, initialClass:^(Ryujinx)$
#
# # archipelago:
# windowrule = workspace 1, initialTitle:^(Archipelago Client)$
#
#
# # windowrule = center, floating:1
# windowrule = float, class:^(pavucontrol)$
# windowrule = float, class:^(blueman-manager)$
# windowrule = float, class:^(nm-connection-editor)$

# layer rules
